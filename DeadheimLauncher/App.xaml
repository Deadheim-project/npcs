<Application x:Class="DeadheimLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="BgBrush" Color="#1E1E22" />
            <SolidColorBrush x:Key="PanelBrush" Color="#2A2A30" />
            <SolidColorBrush x:Key="AccentBrush" Color="#6FA8DC" />
            <SolidColorBrush x:Key="TextBrush" Color="#E8E8EC" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#9A9AA5" />

            <Style TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                <Setter Property="Foreground" Value="#15181C" />
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                <Setter Property="Margin" Value="0,4" />
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Padding" Value="6,4" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
