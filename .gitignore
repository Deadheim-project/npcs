bin/
obj/
*.user
.vs/

# Saída do build do launcher e do instalador
publish/
installer/Output/

# Cache local de pacotes NuGet (usado quando o C: está sem espaço)
.nuget-cache/

# Dump do indice da Thunderstore (153 MB, gerado)
ts_valheim.json

# Fora deste repo: so o mod de NPCs mora aqui
DeadheimLauncher/
installer/
.build-plugins/
.server-test/

# Estado de servidor. Deploy nao mora mais aqui -- este repo constroi o mod e
# para por ai; ver Deadheim-project/deploy. Estas pastas eram produzidas pelos
# scripts que sairam daqui, e ficam ignoradas para que um `git add -A` distraido
# nunca commite WardData.json, VIPplayers.txt ou uma quest viva do servidor.
dist/server-upload/
dist/arcane-backup/
dist/quests-backup/
